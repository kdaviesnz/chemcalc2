
const CMolecule = (mmolecule) => {
   return {
       indexOf : (atom) = {
          // return mmolecule.indexOf(atom)
       },
       push : (atom) = { 
          // mmolecule.push(atom)
          return mmolecule
       },
       delete : (atom) = {
          // mmolecule.delete(atom)
          return mmolecule
       }
   }
}

